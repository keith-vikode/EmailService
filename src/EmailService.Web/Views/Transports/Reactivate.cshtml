@model ActivationViewModel
@{
    ViewBag.Title = $"Reactivate {Model.Name}";
}

<fieldset>
    <form asp-action="Reactivate" asp-controller="Transports" method="post">
        <input asp-for="Id" type="hidden" />
        <input asp-for="Name" type="hidden" />
        <div class="alert alert-warning">Are you sure you want to reactivate this transport?</div>
        <button type="submit" class="btn btn-success">Reactivate</button>
    </form>
</fieldset>